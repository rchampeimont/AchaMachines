To build and install in /usr/local do:
./configure
make
sudo make install

To install in your home directory:
./configure --prefix=$HOME/local
make
make install

Documentation:
The documentation has not yet been written separately from the source code.
So, for now, the documentation is written as comments in the code
at the beginning of classes and methods.
